POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=b2b_radar
POSTGRES_HOST=
POSTGRES_PORT=
WEB_PORT=8000

# Caddy reverse proxy (production deployment)
DOMAIN=example.com
BASIC_AUTH_USER=admin
# Generate with: docker run --rm caddy:2-alpine caddy hash-password --plaintext 'your-password'
BASIC_AUTH_HASH=