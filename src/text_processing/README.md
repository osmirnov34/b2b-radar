# Shared text processing

Small dependency-light language and noise gates used by both online ingestion and offline ML live here. This package
must not import ingestion, ML pipelines, the database, or the web application.
